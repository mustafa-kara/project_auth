<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- QR tarama (mobile_scanner) için kamera. Kamera zorunlu donanım değil
         (manuel otpauth:// girişi de var) → required=false. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <!-- Biyometrik vault kilidi (Patch 5, local_auth_android README zorunlu). -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!-- Supabase (Auth/REST/Realtime) erişimi. debug/profile manifest'lerinde zaten
         var; release build'in ağa çıkabilmesi için ANA manifest'te de ZORUNLU. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Yedekleme/aktarım politikası (güvenlik denetimi P1-2):
         allowBackup=false + fullBackupContent=false BULUT yedeğini kapatır; API 31+
         cihazdan-cihaza (D2D) göçü bazı üreticilerde kapatılamadığı için ayrıca
         dataExtractionRules ile secure_storage'ın SharedPreferences dosyaları hem
         <cloud-backup> hem <device-transfer> dışında bırakılır (gerekçe + dosya
         adlarının kaynağı: res/xml/data_extraction_rules.xml). -->
    <application
        android:label="project_auth"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Faz 3 Patch 1: Supabase e-posta onayı PKCE deep-link callback'i.
                 VIEW + DEFAULT + BROWSABLE ZORUNLU — yoksa tarayıcı linki uygulamaya
                 açamaz. scheme/host SupabaseConfig.authCallbackUrl ile aynı. -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:scheme="dev.mustafakara.projectauth"
                    android:host="login-callback"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
